import 'package:flutter/material.dart';

class ReminderPatientPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Reminders for appointments and hygiene tips go here...',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
