import 'package:flutter/material.dart';

class PatientMessagePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Messages with your dentist go here...',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
